node_modules/
dist/
.env
backend/.env
.DS_Store
*.log
service-account*.json
*-credentials.json
image-staging/

# Raw research input for v2 cultural dataset normalization (not committed —
# see V2-ARCHITECTURE.md "Import preview workflow"). The normalization
# tooling that reads it IS committed; the research text itself is not.
/research-input/
# Import-preview output (regenerated by backend/scripts/v2-import-preview/).
/tmp/
