# ── Build artifacts ────────────────────────────────────────────────────────
node_modules
# Note: dist/ is NOT excluded — multi-stage Dockerfile builds inside container
# and does not rely on a pre-built dist/ from the host.
.vite

# ── Backend source (served separately) ─────────────────────────────────────
backend
data/*
!data/archive.json

# ── Git metadata ───────────────────────────────────────────────────────────
.git
.gitkeep
.gitignore

# ── Docker files (container içinde gerekmez) ───────────────────────────────
Dockerfile
docker-compose.yml
.dockerignore

# ── Editor / OS artıkları ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
*.local

# ── Test / CI artıkları ────────────────────────────────────────────────────
coverage
.nyc_output
