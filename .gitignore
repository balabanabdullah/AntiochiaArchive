node_modules/
dist/
.env
backend/.env
.DS_Store
*.log
service-account*.json
*-credentials.json
image-staging/

# Raw research input for v2 cultural dataset normalization (not committed —
# see V2-ARCHITECTURE.md "Import preview workflow"). The normalization
# tooling that reads it IS committed; the research text itself is not.
/research-input/
# Import-preview output (regenerated by backend/scripts/v2-import-preview/).
/tmp/

# Local-first SQLite runtime + media storage (backend/db/sqliteConnection.js,
# backend/media/mediaStorage.js) — never committed. See backend/PERSISTENCE.md
# "Runtime content database (SQLite)". Contains the live database file, media
# originals/derivatives, and backup/export snapshots — reproducible from
# scripts/migrate-json-to-sqlite.js + the committed canonical JSON, not a
# source of truth to version-control.
backend/var/
